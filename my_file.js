it('Should return \'eight hundred eighty-seven\' when 887 given', () => {
const readable = toReadable(887);
assert.equal(readable, 'eight hundred eighty-seven');
});

it('Should return \'eight hundred eighty-eight\' when 888 given', () => {
const readable = toReadable(888);
assert.equal(readable, 'eight hundred eighty-eight');
});

it('Should return \'eight hundred eighty-nine\' when 889 given', () => {
const readable = toReadable(889);
assert.equal(readable, 'eight hundred eighty-nine');
});

it('Should return \'eight hundred ninety\' when 890 given', () => {
const readable = toReadable(890);
assert.equal(readable, 'eight hundred ninety');
});

it('Should return \'eight hundred ninety-one\' when 891 given', () => {
const readable = toReadable(891);
assert.equal(readable, 'eight hundred ninety-one');
});

it('Should return \'eight hundred ninety-two\' when 892 given', () => {
const readable = toReadable(892);
assert.equal(readable, 'eight hundred ninety-two');
});

it('Should return \'eight hundred ninety-three\' when 893 given', () => {
const readable = toReadable(893);
assert.equal(readable, 'eight hundred ninety-three');
});

it('Should return \'eight hundred ninety-four\' when 894 given', () => {
const readable = toReadable(894);
assert.equal(readable, 'eight hundred ninety-four');
});

it('Should return \'eight hundred ninety-five\' when 895 given', () => {
const readable = toReadable(895);
assert.equal(readable, 'eight hundred ninety-five');
});

it('Should return \'eight hundred ninety-six\' when 896 given', () => {
const readable = toReadable(896);
assert.equal(readable, 'eight hundred ninety-six');
});

it('Should return \'eight hundred ninety-seven\' when 897 given', () => {
const readable = toReadable(897);
assert.equal(readable, 'eight hundred ninety-seven');
});

it('Should return \'eight hundred ninety-eight\' when 898 given', () => {
const readable = toReadable(898);
assert.equal(readable, 'eight hundred ninety-eight');
});

